#cloud-config

packages:
  - epel-release

runcmd:
  - 'yum -y install python-django'
  - 'cd ~ && wget BUILD_URL/artifact/pkg/openstack_app.tar.gz'
  - 'mkdir /usr/share/polls'
  - 'cd /usr/share/polls/ && tar zvxf ~/openstack_app.tar.gz'
  - 'cd /usr/share/polls && python manage.py runserver 0.0.0.0:80'
